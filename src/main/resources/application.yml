server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: video-service
          uri: http://video-service
          predicates:
            - Path=/video/**
        - id: comment-service
          uri: http://comment-service
          predicates:
            - Path=/comment/**
